2. LOGIN PAGE
- Allow login with:
  - Email OR Username
  - Password
- Include:
  - "Remember me" checkbox
- Show proper error states

3. SESSION MANAGEMENT
- Use Supabase Auth for session handling
- Persist login session for at least 7 days
- Auto refresh JWT tokens
- User should NOT need to log in repeatedly

4. AUTH STATE
- Detect if user is logged in
- Redirect:
  - If not logged in → send to /login
  - If logged in → allow access to dashboard
- Protect routes (middleware)

5. USER PROFILE TABLE (Supabase)
Create a table called "profiles":
- id (UUID, linked to auth.users)
- first_name
- last_name
- username (unique)
- email
- created_at

On signup:
- Create user in Supabase Auth
- Insert user data into "profiles" table

-------------------------------------

🎨 UI REQUIREMENTS

- Clean modern UI (like a crypto analytics dashboard)
- Use Tailwind CSS
- Centered auth forms
- Responsive (mobile + desktop)
- Smooth transitions
- Loading states on buttons
- Disable button while submitting

Pages:
- /signup
- /login
- /dashboard (protected)

-------------------------------------

⚙️ TECH SETUP

- Use Supabase JS client
- Use environment variables:
  - NEXT_PUBLIC_SUPABASE_URL
  - NEXT_PUBLIC_SUPABASE_ANON_KEY

- Structure:
  - /app/login/page.tsx
  - /app/signup/page.tsx
  - /app/dashboard/page.tsx
  - /lib/supabaseClient.ts
  - /middleware.ts (for route protection)

-------------------------------------

🧠 EXTRA FEATURES (IMPORTANT)

- On login:
  - If user enters username → resolve it to email before auth
- Show toast notifications for:
  - Success
  - Errors

- Add logout button in dashboard
- Show user's first name in dashboard

-------------------------------------

🚫 DO NOT INCLUDE

- No Privy
- No wallet login
- No OTP-only auth
- No unnecessary backend (Supabase handles backend)

-------------------------------------

🎯 GOAL

The final result should feel like a real SaaS product:
- Fast
- Smooth login
- Persistent sessions
- Professional UX (no repeated verification codes)
