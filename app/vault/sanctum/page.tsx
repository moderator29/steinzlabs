import { SanctumHubClient } from '@/components/vault/sanctum/SanctumHubClient';

export const metadata = { title: 'The Sanctum — Vault — Naka Labs' };

export default function SanctumPage() {
  return <SanctumHubClient />;
}
