# Session Q — Auto-Begin Kickoff

> **This is the only file the next session needs to read.** Bootstrap commands first, then rules, then the complete backlog. Execute top-to-bottom.

---

## 0. Bootstrap (run these EXACTLY, in order, before doing anything else)

```bash
cd "c:/Users/DELL LATITUDE 5320/Downloads/steinzlabs"

# 1. Sync main + prune merged remotes
git checkout main && git pull --ff-only
git fetch --prune

# 2. Verify identity (CLAUDE.md requirement — NEVER skip)
git config user.name   # must print: moderator29
git config user.email  # must print: 101205446+moderator29@users.noreply.github.com
# If wrong, fix:
# git config user.name "moderator29"
# git config user.email "101205446+moderator29@users.noreply.github.com"

# 3. List unmerged branches owner needs to merge (expect 12 from Sessions O+P)
git branch -r | grep -v 'origin/main$' | grep -v HEAD

# 4. Confirm Supabase MCP works (smoke test)
# Will be called automatically when needed; just verify env is healthy.

# 5. Read the operating rules below + the backlog in §3.
# Then cut your first phase branch and start.
```

**If `git config user.name` is NOT `moderator29` — STOP and fix it. Every commit attribution depends on this.**

---

## 1. Operating rules (auto-applied — do NOT ask, just execute)

These are locked in auto-memory and govern every session on this repo. Re-listing here for completeness so this kickoff is self-contained:

### From `CLAUDE.md` (hard rules)
- **Author all commits as** `moderator29 <101205446+moderator29@users.noreply.github.com>`. Verify before first commit.
- **Never reference Claude / Anthropic / "Generated by" / "AI-assisted"** anywhere — commits, code, docs, PR descriptions, branch names. No `claude/`, `ai/`, `claude-code/` branch prefixes.
- **Never commit to main.** Branch + push + STOP. Owner merges via GitHub UI. `gh` CLI is not installed and that's intentional.
- **Conventional Commits**: `feat:` / `fix:` / `refactor:` / `chore:` / `docs:` / `security:` / `perf:` / `test:` / `style:`.
- **No mock/fake/demo data.** Real APIs only (CoinGecko, Alchemy, Helius, GoPlus, Jupiter, 0x, Anthropic, Supabase, Birdeye, Etherscan v2, Honeypot.is, de.fi, RugCheck). If data is unavailable: empty state + error, never fabricated numbers. **This is a CLAUDE.md hard rule.**
- **No `any` types** unless documented. **No `console.log` in prod code** (use `lib/logger.ts` Pino). **No commented-out code.** **No empty `try/catch` blocks.**
- **Address normalization**: always use `lib/utils/addressNormalize.ts`. Never call `.toLowerCase()` on a raw wallet/token address — Solana is case-sensitive.
- **Schema gotchas**: `whales.label` (not `name`), `price_alerts.price` (not `target_price`), `user_wallets_v2.wallets` is JSONB. Verify column names against the live DB via `mcp__supabase__list_tables` / `execute_sql` — migration files can be stale.
- **Supabase migrations** via `mcp__supabase__apply_migration` + mirror SQL into `supabase/migrations/`. Owner has granted blanket migration approval in autonomous mode but **destructive ops still need explicit confirmation**.
- **Never bypass RLS via `service_role`** from a client-callable endpoint without explicit user-id binding.

### From auto-memory (this session)
- **Autonomous mode**: never stop, never ask, never give an opinion. Industry-standard defaults applied silently. Pause only for genuinely destructive non-migration ops (mass branch delete, DROP/TRUNCATE on prod data, force-push to existing PR branches, dependency major-version bump with breaking changes).
- **Branch consolidation**: for big multi-section prompts, bundle into 3–5 phase branches max. "No too much of branches and always deliver cleanly."
- **No-stub build-the-pipeline**: UI missing real data = BUILD the server pipeline in the SAME branch (real upstream API). Never park, never stub.
- **Full-prompt extraction**: when given a numbered-section prompt, pull EVERY sub-bullet into the TodoWrite list. Section-level summaries get caught. Re-scan the prompt before declaring scope complete.
- **Overnight mode**: when owner says "going to bed" / "wake up still working" / "no stopping" — parallelize via Agents on independent work (audits, doc generation, code searches), ship every backlog item in single session, status-update only on branch push or genuine blocker.

### Quality bar (from `steinz_labs_rules.md`)
- Top 1% MEVX/Nansen/Trojan/Maestro grade — exceed, not match.
- TS clean + build clean + 0 console errors + sub-1s page load + sub-2s actions before declaring complete.
- WCAG AAA contrast (7:1 normal, 4.5:1 large). Body 15-16px min, weight 500-600.
- Mobile 375 / 393 / 412 + tablet 768 / 1024 + desktop 1440 / 1920 all perfect.
- Log every paid API call (cost visibility).
- Audit every shipped section: empty try/catches, console.logs in prod, TODOs, hardcoded values, missing loading/error states, broken pagination, memory leaks, N+1 queries.

### Communication
- Casual tone with owner. No corp-speak.
- TodoWrite a real plan before any code.
- "Todo list" from owner = the FULL backlog in plain English, he picks.
- Be honest about scope and blockers. "Wired, awaiting smoke test" is better than overclaiming.
- Never lead with "great question" / "absolutely" / sycophancy.

---

## 2. State of the world entering Session Q

### Branches awaiting merge (12 total, suggested order)

1. `refactor/naka-wallet-unification` — 6 consumers → useNakaWallet
2. `phase/vtx-ai-consumer-flip` — VTX primitives in VtxAiTab
3. `phase/security-panel-assembly` — LpLockPanel mount
4. `feat/social-foundation` — 8 social tables + libsodium + 11 API routes (migration LIVE in prod DB)
5. `feat/social-profile-and-discovery` — /discover + 8 leaderboards + /u/[username] + encrypted DMs
6. `feat/onboarding-and-security-pipelines` — 10-card onboarding + Triangulation + Deployer-history pipelines (migration LIVE)
7. `feat/reputation-cron-admin-cult` — 5-component scorer + nightly cron + 5 admin pages + Phase 9 cult threshold reader
8. `chore/audit-sweeps-and-cves` — industry-standard-audit.md + Pino logger + CSS tokens + 4 nav-state pages
9. `chore/seo-locale-foundation` — next-intl config + base bundles + migration playbook
10. `docs/session-o-handoff-and-docs` — Session O handoff + public docs + admin docs + VTX learning + CS templates
11. **`fix/comprehensive-gap-fill`** — Session P 21-fix gap-fill (migration LIVE) — MERGE LAST
12. `docs/session-q-kickoff` — THIS doc — merge after #11

Expected conflict: `components/ProfileTab.tsx` (Branch B + fix branch both edit — both add, no deletions to same lines, should auto-merge). `components/market/SecurityPanel.tsx` (#3 adds LpLockPanel, #6 adds Triangulation + DeployerHistory; keep all three render blocks in order).

### Migrations applied to prod DB this session arc (already LIVE, mirrored to `supabase/migrations/`)
- `2026_05_16_social_layer_foundation` — 8 social tables + profile cols + RLS + Realtime publication + platform_settings.naka_threshold
- `2026_05_16_onboarding_and_security_caches` — onboarding_events + security_source_verdicts + deployer_history_cache
- `2026_05_17_rls_holes_and_social_rpcs` — push_delivery_log + threats RLS + auto_copy enum + dedup index + social_top_followers + social_block_analytics RPCs

All idempotent (IF NOT EXISTS / DROP POLICY IF EXISTS), safe to re-run.

### Env vars to set in Vercel BEFORE merging branch #7
- `CRON_SECRET` — required by `/api/cron/recompute-reputation`; cron 403s without it.
- `ETHERSCAN_API_KEY` (optional but recommended) — full EVM deployer-history coverage. Helius key already present.

---

## 3. Complete carry-forward backlog

Everything NOT done across Sessions O+P. Ordered by suggested branch grouping so you can pick a branch, work it, ship it, repeat. Each item has file:line refs from the 20-agent audit so you can dive in cold.

### Branch H1: `fix/audit-followups-trading-money` (~6h)

**Sniper Bot — Agent 4 findings:**
- `sniper-timing-instrumentation` — wrap relay calls in `app/api/sniper/execute/route.ts:115-134` with `Date.now()` so `execution_time_ms` populates. Currently always null; sub-2s claim unprovable.
- `sniper-relayer-failure-handling` — replace silent `.catch(() => {})` at `execute/route.ts:125-132` with retry + Sentry capture. User never learns when relayer fails.
- `sniper-tp-sl-status` — `lib/sniper/autosell.ts` exists but executor status isn't surfaced in the UI. Add a "monitoring active" indicator to the sniper card and a "stops triggered" history log.
- `sniper-create-review-modal` — `NewSniperModal.tsx:128-175` saves directly; competitors (Maestro, Banana Gun, Trojan) all show a review step. Add a confirmation pane with slippage / TP / SL summary before commit.

**Copy-Trading — Agent 5 findings:**
- `copy-trade-mode-rewire` — `/api/cron/copy-trade-monitor/route.ts:62-133` reads `user_whale_follows.copy_mode` but should read `user_copy_rules.mode` (auto_copy enum now exists). UI mode switcher is currently ineffective.
- `copy-trade-audit-log` — `/api/copy-trading/rules/[id]` PATCH/DELETE + `execute/route.ts` write zero audit-log rows. Add `admin_audit_log` writes for every rule edit + execution.

**Portfolio — Agent 3 findings:**
- `portfolio-chart-label-fix` — `/app/api/portfolio/performance/route.ts:151-168` returns capital flow, not mark-to-market PnL. Either rename the chart "Capital flow" or implement real PnL via Alchemy transfer history.
- `portfolio-timestamp-bug` — `/app/api/portfolio/performance/route.ts:163` divides by `86_400_000` (ms→days) but returns `* 86_400` (seconds). Should be `* 86_400_000`.
- `portfolio-mobile-donut` — `/app/dashboard/portfolio/page.tsx:323-333` donut chart fixed `h-64`, no `md:` breakpoint; legend illegible on 375px.
- `portfolio-performance-empty-catch` — `/app/dashboard/portfolio/page.tsx:135` `.catch(() => {})` swallows errors. Replace with error UI + Sentry capture.

**Smart-Money convergence — Agent 11 findings:**
- `smart-money-convergence-table` — no `smart_money_convergence` table; convergence is in-memory from `recentTrades` array in `/app/api/smart-money/route.ts:312-333`. Build the materialized view.
- `populate-whale-score` — `wallet_profiles.whale_score` + `wallet_clusters.whale_score` columns exist but never written. Need on-chain clustering cron to populate.

### Branch H2: `fix/audit-followups-platform` (~4h)

**Crons — Agent 16 findings:**
- `cron-stubs-cleanup` — delete or schedule these 7 handlers (currently have route files but no `vercel.json` entry): `fear-greed-index`, `narrative-detection`, `network-metrics`, `smart-money-ranking`, `trends-aggregator`, `whale-ranking-refresh`, `alert-monitor`.
- `sniper-cron-admin-override-review` — `app/api/cron/sniper-auto-execute/route.ts:32-33` accepts `ADMIN_MIGRATION_SECRET` without `CRON_SECRET`. Audit whether this is intentional.
- `cron-sentry-alerting` — failures go to `cron_execution_log` table (Agent 9) but never to Sentry. Add `Sentry.captureException` on every cron error path.

**Rate-limiting + CSRF retrofit — Agent 17 findings:**
- `guard-route-retrofit` — apply `lib/api/guardRoute.ts` (already shipped) to: `/api/alerts` POST/DELETE, `/api/copy-trading/rules` POST, `/api/copy-trading/execute` POST, `/api/support` POST, `/api/user/delete` DELETE, `/api/wallet/send` POST. ~30 min mechanical.
- `rate-limiter-consolidation` — three different limiter implementations coexist (`lib/rateLimit.ts`, `lib/rateLimit/rateLimit.ts`, `lib/security/rateLimit.ts`). Consolidate to the Upstash-backed `lib/rateLimit/rateLimit.ts` and delete the in-memory ones.

**Telegram + push — Agent 6 findings:**
- `telegram-delivery-queue` — `lib/telegram/notify.ts:82-84` is fire-and-forget. Build Upstash queue + 3-retry exponential backoff + persist failures to a `telegram_delivery_failures` table.
- `vapid-key-rotation` — `lib/preferences/webPush.ts:27` hardcodes the public key from env. Build versioned key storage in DB with grace-period overlap.
- `notification-dedup` — `/api/notifications` POST has no idempotency check. Same event posted twice = push + email + telegram all fire twice. Add event_id check before insert.
- `notification-rate-limit` — `/api/notifications` POST has no endpoint rate limit. Bot can spam 1000 in 1 second. Add per-user 100/60s.

**Auth — Agent 2 findings:**
- `auth-sentry-tagging` — 7 catch blocks across auth routes log to console but don't tag Sentry: signin (line 81-84), lookup (line 39-41), confirm-user (line 21-24), forgot-password (line 46-49), resend-verification (line 42-45), reset-password (line 40-44), verify-email (line 54-56, 60-63). Add `Sentry.captureException(err, { tags: { route: '...', phase: '...' } })` to each.

### Branch H3: `fix/audit-followups-ui-and-a11y` (~6h)

**Accessibility — Agent 8 findings:**
- `modal-focus-traps` — `AlertModal.tsx:34`, `BuySellModal.tsx:106` don't trap focus; Tab can escape to content behind. Add `focus-trap` (react-focus-lock or similar).
- `aria-labels` — 8 icon-only buttons missing `aria-label`: `ChartToolbar.tsx:66-77` (4 buttons), `NotificationBell.tsx:158`, `AlertModal.tsx:41`, `BuySellModal.tsx:113`, `FiltersModal.tsx:101`.
- `aria-pressed` — `TimeframeSelector.tsx:16` buttons missing `aria-pressed`; sort `<th>` cells in `PortfolioTable.tsx:79` missing role/aria-label.
- `text-slate-500-600-sweep` — 12+ instances of `text-slate-500/600` failing AAA on dark bg. Files: `PortfolioHistoryPanel.tsx:77,84,102,106,112,158`, `FiltersModal.tsx:151`, `PageHeader.tsx:24`, `NotificationBell.tsx:162`, `PricesTable.tsx:80,90`.
- `prefers-reduced-motion` — `FiltersModal.tsx:93` (slide-in), `NotificationBell.tsx:207` (animate-spin), `HealthBadge.tsx:80` (animate-pulse), `ProfileTab.tsx:1020-1022` (animate-bounce loader). Wrap each in `@media (prefers-reduced-motion: no-preference)`.

**Error boundaries — Agent 15 findings:**
- `dynamic-route-boundaries` — `error.tsx` + `not-found.tsx` shipped on `app/dashboard/` and `app/admin/` root, but NOT on dynamic `[id]` segments (whale detail, cluster detail, share, coin/[chain]/[address], etc.). Add to each.
- `PlatformEventMonitor-timeout` — `components/PlatformEventMonitor.tsx:131-148` fetch chains have no `signal: AbortSignal.timeout(...)`. Add 10s timeout.

**Hex sweep — Agent 21 findings + earlier audit:**
- `hex-sweep-top-5` — Per the agent's density ranking, prioritize: `app/dashboard/wallet-intelligence/page.tsx` (185 hex), `components/ProfileTab.tsx` (158), `app/admin/page.tsx` (152), `app/dashboard/wallet-page/page.tsx` (134), `app/dashboard/dna-analyzer/page.tsx` (132). Replace per the token map in `docs/industry-standard-audit.md` §1: `#0A1EFF → var(--nl-blue)`, `#10B981 → var(--nl-success)`, `#1E2433 → var(--nl-border-dark)`, etc.

**i18n surface migration — Agent 14 findings:**
- `app/[locale]-surface-migrations` — Foundation (next-intl plugin + config + bundles) is shipped on `chore/seo-locale-foundation`. Run the playbook in `docs/seo-locale-migration.md` per surface group in this order: social (discover, leaderboard, u) → portfolio → whale-tracker → vtx-ai → wallet-page → marketing → remaining dashboard. One PR per group. Add `next-intl/plugin` to `next.config.js` with the first surface.
- `top-20-hardcoded-strings` — `components/landing/cards-data.ts` (24 strings), `components/landing/FAQData.ts` (12 Q&A pairs), `components/landing/SecurityShowcase.tsx` (16 strings), `LandingNav.tsx` (7 nav labels). Migrate to `lib/i18n/messages/en.json` namespaces.
- `tolocale-string-locale-param` — 121 `.toLocaleString()` calls across codebase lack explicit `locale` param. Add `locale` from `useLocale()` next-intl hook.
- `rtl-logical-properties` — Zero `start`/`end` CSS — all `left`/`right`. Migrate to logical properties for RTL readiness.

### Branch H4: `fix/audit-followups-profile-and-settings` (~3h)

**Profile / Settings — Agent 12 findings:**
- `settings-replay-onboarding-button` — `/app/settings/page.tsx` has no replay button. Add one that calls `UPDATE profiles SET onboarding_completed_at = NULL WHERE id = auth.uid()` so `<OnboardingGate>` re-mounts.
- `privacy-panel-rewire` — `components/ProfileTab.tsx:866-917` writes to `user_metadata.privacy_*`. Should write to the discrete columns added in Branch A: `profiles.is_private`, `dm_permission`, `show_success_rate`, `show_wallet_balance`, `show_activity`. Migrate the read path too.
- `2fa-implementation` — `ProfileTab.tsx:630-640` is a "COMING SOON" stub. Implement TOTP (otplib) + optional WebAuthn passkey.
- `connected-wallets-edit` — `/app/settings/page.tsx:226-233` is read-only. Build add/remove/rename UI hitting `user_wallets_v2`.
- `gdpr-export-ui` — Endpoint `/api/account/export/route.ts` works; no UI button linked to it. Add a "Download my data" button in Settings → Account.

**Profile page misc:**
- `username-from-profile` — `FollowersTile` in Branch H navigates to `/u/${username}` but reads username from `user.user_metadata.username` — better path is `profiles.username` via Supabase fetch. Cache on the user object.

### Branch H5: `fix/audit-followups-wallet-completeness` (~4h)

**Wallet — Agent 4 findings (partly addressed in Branch H):**
- `walletTab-balances-wire` — `/api/wallet/balances` shipped on Branch H but `components/WalletTab.tsx:37` still calls `/api/token-scanner`. Switch to `/api/wallet/balances?address=...&chain=...`.
- `wallet-nft-tab-mount` — `components/wallet/NftTab.tsx` shipped on Branch H; mount in `app/dashboard/wallet-page/page.tsx` as a new tab between Holdings + Activity.
- `transaction-decoding` — `ActivityTab.tsx:2615-2680` reads only `localStorage`. Replace with `/api/wallet/transactions` route that uses Etherscan v2 + Helius transaction parsing for Rabby-grade decoded calls. Cache in existing `transaction_history` table.
- `wallet-in-app-swap` — `/dashboard/coin/[chain]/[address]/page.tsx:19` "Coming Soon" modal. Build lite 0x/Jupiter swap form.

### Branch H6: `fix/audit-followups-vtx-and-pino` (~4h)

**VTX — Agent 3 findings:**
- `vtx-page-streaming` — `/app/dashboard/vtx-ai/page.tsx:504` doesn't pass `stream: true`. Wire streaming on the page-level component matching what VtxAiTab does.
- `vtx-tool-event-streaming` — Tool calls force JSON fallback. Modify `/api/vtx-ai` to emit SSE events `{ type: 'tool_use', name, status }` during tool execution. Refactor `VtxToolSidecar` to consume real-time events instead of inferring from message history.
- `vtx-portfolio-context` — On authenticated requests, query `positions` or `wallet_summary` and inject `User Portfolio: N tokens, $X` into the system prompt. ~30 lines.
- `vtx-bubble-on-page` — `BubbleVisualization` exists in `VtxAiTab` but not on the page-level component. Copy the render block.

**Pino sweep — Branch E foundation:**
- `pino-top-20-routes` — `lib/logger.ts` shipped on Branch E. Convert these 20 highest-frequency `console.*` routes first: every cron handler, `app/api/swap/*`, `app/api/sniper/execute`, `app/api/copy-trading/execute`, `app/api/wallet/*`, `app/api/vtx-ai/*`. Each: `import { routeLogger } from '@/lib/logger'; const log = routeLogger('/api/...');` then `log.info({...}, 'msg')` / `log.error({err, ...}, 'msg')`.

### Branch H7: `fix/audit-followups-admin-and-research` (~3h)

**Admin — Agent 13 findings:**
- `admin-audit-log-viewer` — `admin_audit_log` table has data; no page renders it. Build `/admin/audit-log` listing every action with filter by actor / target / time range.

**Research / Community / Social-Trading — Agent 19 findings:**
- `community-page-real-data` — `/app/dashboard/community/page.tsx` is 100% static placeholder; per master prompt §1.1 scope note these are MAIN platform features and should be functional. Wire to a real feed (forum or Discord embed or social_follows-based activity stream).
- `social-trading-real` — `/app/dashboard/social-trading/page.tsx` shows waitlist + hardcoded PLANNED_FEATURES. Either ship the real surface or remove from nav until ready.
- `launchpad-mobile-fix` — `grid-cols-4` clips at 375. Change to `grid-cols-2 sm:grid-cols-4`.
- `research-empty-catch` — `app/dashboard/research/page.tsx:308` empty catch. Add error UI.

### Branch H8: `fix/audit-followups-market-data` (~3h)

**Market data sources — Agent 18 findings:**
- `orphaned-fetch-consolidation` — 6 unauthenticated fetch calls bypass structured services (no retry / cache / cost log): `app/admin/page.tsx:197`, `app/api/market/route.ts:37`, `app/api/health/route.ts:58`, `app/api/context-feed/route.ts:187,605,640`, `app/api/predictions/route.ts:305`. Route each through the relevant `lib/services/*` wrapper.
- `cost-logging-all-providers` — `api_logs` writes happen for sniper paths only. Wrap every paid API call (CoinGecko, Alchemy, Helius, GoPlus, 0x, Birdeye) so cost telemetry covers the whole platform.
- `zerox-retry-backoff` — `lib/services/zerox.ts:118-127, 189-198` no retry on 5xx; direct throw. Add exponential backoff (3 tries, 250 / 500 / 1000ms).
- `jupiter-retry` — `lib/services/jupiter.ts:113-116, 174-175, 199` returns `null` on error. Add retry + Sentry capture before falling back.

### Branch H9: `fix/audit-followups-landing-seo` (~2h)

**Landing — Agent 1 findings:**
- `og-image-url-fix` — `app/layout.tsx:44` falls back to `steinzlabs.vercel.app`; should be `nakalabs.xyz`.
- `landing-generateMetadata` — `app/page.tsx` uses global metadata only. Add `generateMetadata()` with landing-specific title/description.
- `floating-coins-next-image` — `components/landing/FloatingCoins.tsx:8-17` uses raw `<img>` from `assets.coingecko.com`. Migrate to `next/image` with `priority`, `width/height`, `loader`. Cuts CLS + fixes mobile (currently `hidden md:block`).
- `stars-mobile-prune` — `HeroSection.tsx:8-14` renders 55 inline animated divs on all viewports. Reduce to 30 on mobile via `useMediaQuery`.
- `social-icons-contrast` — `LandingFooter.tsx:46,52` social icons use `text-white/40` (≈2.1:1 fail). Bump to `text-white/70`.
- `vtx-section-contrast` — `VTXSection.tsx:42` `#8899cc` (4.2:1 fails AAA). Raise to `#6d85ff` or token equivalent.

### Branch H10: `fix/audit-followups-cleanup` (~2h, lowest priority)

**Schema — Agent 21 findings:**
- `dead-tables-cleanup` — These tables are never referenced by app/lib code. Either drop or document why kept: `cult_ambient_tracks`, `cult_cosmetics`, `cult_member_loadouts`.
- `migration-naming-hygiene` — 6 migrations share `2026_05_02_*` prefix. Not functional but rollback-confusing. Rename for ordering clarity in a future cleanup migration.

**Browser smoke tests (Section 10 from master prompt):**

After all branches merge, run through these 20 critical flows. None have been browser-verified yet:
1. Signup → onboarding cards 1-10 → completes → lands on dashboard
2. Skip onboarding → confirms → lands on dashboard → replays from Settings
3. /discover → leaderboards load → click user → profile loads
4. Profile → Follow → button updates + count updates
5. Own profile → Following container → list → unfollow → updates
6. Own profile → Followers container → list
7. Search user → autocomplete → click result → profile opens
8. Bottom-nav Find button → opens /discover
9. Try DM non-mutual → button disabled with tooltip
10. Mutual follow → Message → conversation opens
11. Send encrypted message → recipient receives → reads → marked read
12. Block user → can't DM, invisible in lists
13. Set account private → follow requires approval
14. Report user → admin sees in /admin/social-reports
15. /admin/audit-tracker shows audit findings
16. Profile container 4 (Followers) and 5 (Following) display correctly
17. Followers/Following list pagination works
18. Success-rate badge calculates after cron run
19. All leaderboards refresh in real-time
20. Mobile responsive on every new page (375 / 393 / 412 / 768 / 1024 / 1440 / 1920)

### Section 10 security tests
- Verify DM messages cannot be read server-side (decrypt with `service_role` → fails because conversation key only sealed to participants).
- Verify blocked users genuinely cannot interact (search, follow, DM all gate).
- Verify private account follow requests require approval (status='pending' until target PATCHes 'accept').
- Verify RLS cross-user isolation: User A cannot SELECT User B's `social_blocks` rows.
- Verify rate-limit kicks in at 30 follows/hour, 60 DMs/minute, 5 reports/hour.
- Verify report-reporter-identity hidden from reported user (no notification leaks reporter_id).

### Section 10 performance tests (run Lighthouse CI)
- Cold page load < 1.5s
- Warm page load < 500ms
- API response < 800ms
- Interactive response < 100ms
- Animation 60fps
- TTI < 2s

Add `npm run perf:lighthouse` script that exits non-zero below threshold.

### Out-of-this-arc backlog (longer-horizon)
- **NFT marketplace beyond display** (buy/sell from within wallet)
- **Forward secrecy on DMs** — rotate conversation keys monthly
- **Device recovery flow** — restore DM keys after session reset
- **Apple/Google Pay integration** — currently zero payment provider wired
- **Webhook signature verification** for Stripe/Coinbase Commerce when payments go live
- **Refund/cancellation path** for subscriptions

---

## 4. How to start Session Q (literal first 5 actions)

1. Run §0 bootstrap commands.
2. Re-read §1 operating rules until they're fresh.
3. Build a TodoWrite list from §3 — every sub-bullet a separate todo. Per the full-prompt-extraction rule.
4. Cut **`fix/audit-followups-trading-money`** as the first phase branch. Highest user-impact + clearest scope (no infra dependencies).
5. Start executing. Don't ask. Don't opine. Ship.

Recommended branch order (by impact × independence):
H1 (trading money) → H2 (platform / rate-limits / crons) → H5 (wallet completeness) → H6 (VTX + Pino) → H3 (a11y + i18n surfaces) → H4 (profile/settings) → H7 (admin + research) → H8 (market data) → H9 (landing SEO) → H10 (cleanup)

Each branch typecheck-clean + audit-pass before push.

---

## 5. After every branch ships

Update this kickoff doc's §3 by checking off completed items + adding any NEW findings discovered while implementing. The kickoff is the rolling source of truth.

When all 10 H-branches are shipped: write Session R kickoff, mark this session as completed, and start the next prompt arc.

---

## 6. Emergency rollback path

If something breaks in prod after merge:
- All Session O+P migrations are additive (no DROP COLUMN, no DROP TABLE, no data migration). Safe to leave applied; the code paths consuming them are gated to handle absence (e.g., `social_top_followers` RPC has a client-side fallback).
- Each branch is independently revertable via GitHub. Suggested partial-rollback order: revert H-branches first (newest fixes) → revert chore/ branches → revert feat/ branches → only revert refactor/ as a last resort.
- Sentry alerts on every error path now have route + req_id tagging — search by tag instead of grepping logs.

---

Owner: ship and verify. Future-me: pick branch H1, follow the rules, don't ask.
