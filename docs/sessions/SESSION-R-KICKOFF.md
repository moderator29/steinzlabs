# Session R — Auto-Begin Kickoff

> **This is the only file the next session needs to read.** Run §0 verbatim, then §1 (rules), then §2 (what's hot RIGHT NOW), then §3 (the carry-forward backlog). No re-discovery.

---

## 0. Bootstrap (run these EXACTLY, in order, before anything else)

```bash
cd "c:/Users/DELL LATITUDE 5320/Downloads/steinzlabs"

# 1. Sync main + prune merged remotes
git checkout main && git pull --ff-only
git fetch --prune

# 2. Verify identity (CLAUDE.md requirement — NEVER skip)
git config user.name   # must print: moderator29
git config user.email  # must print: 101205446+moderator29@users.noreply.github.com
# If wrong:
# git config user.name "moderator29"
# git config user.email "101205446+moderator29@users.noreply.github.com"

# 3. List unmerged branches the owner still needs to merge
git branch -r | grep -v 'origin/main$' | grep -v HEAD

# 4. Verify the typecheck + build are clean BEFORE making any new changes
npx tsc --noEmit -p .   # must report 0 errors
# Production build is heavy (~3 minutes). Run only when you're about to
# claim "shipped" — Vercel will do its own build on merge.
# SENTRY_SUPPRESS_TURBOPACK_WARNING=1 npx next build

# 5. MCP smoke test
# Supabase + Vercel MCP tools come from the deferred-tools list. Load via
# ToolSearch only when needed:
#   ToolSearch query="select:mcp__supabase__list_tables,mcp__supabase__execute_sql,mcp__supabase__apply_migration"
#   ToolSearch query="select:mcp__vercel__get_deployment,mcp__vercel__list_deployments"
# Owner has granted blanket migration approval — additive migrations
# (ADD COLUMN IF NOT EXISTS, CREATE INDEX, CREATE TABLE) proceed without
# asking. Destructive ops (DROP, TRUNCATE on prod data, force-push to
# existing PR branches) still require explicit confirmation.
```

**Identity check is non-negotiable.** Every commit attribution depends on it. Sessions in the past have shipped with the wrong author and required a history rewrite.

---

## 1. Operating rules (auto-applied — do NOT ask, just execute)

### From `CLAUDE.md` (hard rules — read it on session start)
- **Author all commits as** `moderator29 <101205446+moderator29@users.noreply.github.com>`. Verify before first commit.
- **Never reference Claude / Anthropic / "Generated by" / "AI-assisted"** anywhere — commit messages, code, comments, docs, PR descriptions, branch names. No `claude/`, `ai/`, `claude-code/` branch prefixes.
- **Never commit to main.** Branch + push + STOP. Owner merges via GitHub UI. `gh` CLI is not installed and that's intentional.
- **Conventional Commits**: `feat:` / `fix:` / `refactor:` / `chore:` / `docs:` / `security:` / `perf:` / `test:` / `style:`.
- **No mock/fake/demo data.** Real APIs only (CoinGecko, Alchemy, Helius, GoPlus, Jupiter, 0x, Anthropic, Supabase, Birdeye, Etherscan v2, Honeypot.is, de.fi, RugCheck). If data is unavailable: empty state + error UI, never fabricated numbers.
- **No `any` types** without justification. **No `console.log`** in prod code (use `lib/logger.ts` Pino). **No commented-out code.** **No empty `try/catch` blocks.**
- **Address normalization**: always `lib/utils/addressNormalize.ts`. Never `.toLowerCase()` on a raw wallet/token address — Solana is case-sensitive.
- **Schema gotchas**: `whales.label` (not `name`), `price_alerts.price` (not `target_price`), `user_wallets_v2.wallets` is JSONB. Verify against the live DB via `mcp__supabase__list_tables` / `execute_sql` — migration files can be stale.
- **Supabase migrations** via `mcp__supabase__apply_migration` + mirror SQL into `supabase/migrations/`. Blanket approval for additive ops; destructive ops require explicit confirmation.
- **Never bypass RLS via `service_role`** from a client-callable endpoint without explicit user-id binding.

### From auto-memory (carry-over from Sessions O–Q)
- **Autonomous mode**: never stop, never ask, never opine. Industry-standard defaults applied silently. Pause only for genuinely destructive non-migration ops (mass branch delete, DROP/TRUNCATE on prod data, force-push to existing PR branches, dependency major-version bumps).
- **Branch consolidation**: bundle multi-section work into 3–5 phase branches. "No too much of branches."
- **No-stub build-the-pipeline**: UI missing real data = BUILD the server pipeline in the SAME branch (real upstream API). Never park, never stub.
- **Full-prompt extraction**: numbered-section prompts → pull EVERY sub-bullet into the TodoWrite list. Re-scan the prompt before declaring scope complete.
- **Overnight mode**: when owner says "going to bed" / "wake up still working" / "no stopping" — ship every backlog item in single session, status-update only on branch push or genuine blocker.
- **Todo list = full backlog in plain English**, then the owner picks.

### Quality bar
- Top 1% MEVX/Nansen/Trojan/Maestro grade — exceed, not match.
- TS clean + build clean + 0 console errors + sub-1s page load + sub-2s actions before declaring complete.
- WCAG AAA contrast (7:1 normal, 4.5:1 large). Body 15–16px min, weight 500–600.
- Mobile 375 / 393 / 412 + tablet 768 / 1024 + desktop 1440 / 1920 all perfect.
- Log every paid API call (cost visibility).
- Audit every shipped section: empty try/catches, console.logs in prod, TODOs, hardcoded values, missing loading/error states, broken pagination, memory leaks, N+1 queries.

### Communication
- Casual tone. No corp-speak.
- TodoWrite a real plan before any code.
- Be honest about scope and blockers. "Wired, awaiting smoke test" beats overclaiming.
- Never lead with "great question" / "absolutely" / sycophancy.

### Auto-memory files (already loaded at session start, no need to re-read)
- `steinz_labs_user.md` — owner profile (Phantomfcalls, founder/CEO, casual tone)
- `steinz_labs_rules.md` — real APIs only, top-1% grade, AAA contrast
- `steinz_labs_autonomous_mode.md` — no stopping, no asking, industry defaults
- `steinz_labs_branch_consolidation.md` — 3–5 phase branches max
- `steinz_labs_no_stub_build_pipeline.md` — build the real server pipeline
- `steinz_labs_full_prompt_extraction.md` — every sub-bullet → todo
- `steinz_labs_overnight_mode.md` — ship every backlog item end-to-end
- `steinz_labs_schema_gotchas.md` — column-name surprises
- `steinz_labs_naka_token.md` — ETH-only, `0x6967b9a8c0b14849CFE8f9E5732B401433fD2898`, threshold 1,227,000
- `steinz_labs_brand_icons.md` — W is colour ref; rocket/helmet/pentagon are platform icons
- `steinz_labs_todo_style.md` — full backlog, then owner picks
- `steinz_labs_session_r_kickoff.md` — pointer to THIS file (write it on first run)

---

## 2. State of the world entering Session R

### 🚨 P0 — UNMERGED EMERGENCY FIX (merge FIRST, before anything else)

`fix/emergency-vtx-and-dashboard-revert` — fixes the **infinite spinner on `/dashboard`**. Root cause: `app/dashboard/page.tsx:167` called `useSearchParams()` but the import on line 13 only pulled `useRouter`. ReferenceError at render, ErrorBoundary swallowed it, user saw a permanent spinner. **One-line import fix. Merge this first.**

### Other known prod issues (need owner-side console logs to diagnose)
- **`/dashboard/vtx-ai` "half-rendered" report** — sidebar + input bar visible, middle area empty. The page imports `useSearchParams` correctly. Probably a localStorage-history render issue OR the welcome card hiding when `messages.length > 1` after a sync from Supabase. **Next session: ask owner to share the browser console errors from this page so we can pinpoint.**

### Branches awaiting merge (verify with `git branch -r`)

P0 first:
1. **`fix/emergency-vtx-and-dashboard-revert`** — `/dashboard` infinite-spinner fix (one-line import). **MERGE FIRST.**

Then the audit follow-up branches (independent — pick by impact):
2. `fix/audit-followups-trading-money` — H1 (sniper timing + relayer retry, copy-trade rewire, portfolio fixes, smart-money table)
3. `fix/audit-followups-ui-and-a11y` — H3 core (focus traps, aria, contrast, error boundaries, fetch timeouts)
4. `fix/audit-followups-profile-and-settings` — H4 (replay-onboarding + GDPR export button)
5. `fix/audit-followups-wallet-completeness` — H5 (balances rewire, NftTab mount, decoded tx history, lite swap modal)
6. `fix/audit-followups-i18n-and-rtl` — H3 deferred (RTL Tailwind sweep on 190 files, en.json landing namespace, formatters helper)
7. `fix/preexisting-tsc-errors` — clears the 11 pre-existing tsc errors + Next 16 config hygiene + libsodium install
   - **Note**: conflicts with `fix/audit-followups-platform` on `lib/preferences/webPush.ts`. Resolution = keep the `BufferSource` cast + the rotating-VAPID logic.

Already merged this arc (don't touch unless reverting):
- `fix/audit-followups-platform` (#405) — H2
- `fix/audit-followups-market-data` (#404) — H8
- `fix/audit-followups-cleanup` (#402) — H10
- `fix/audit-followups-admin-research-seo` (#401) — H7 + H9
- Dependabot bumps: @wagmi/connectors 6.2.0, @anthropic-ai/sdk 0.96.0, @types/node, @supabase/supabase-js, pg 8.21.0

### Migrations applied to prod DB this arc (already LIVE, mirrored to `supabase/migrations/`)
- `2026_05_17_sniper_executions_buy_amount_usd` — sniper safety-gate USD column + user/executed_at index
- `2026_05_17_telegram_delivery_failures` — telegram retry queue
- `2026_05_17_vapid_keys` — versioned VAPID key storage
- (Plus prior arc migrations: social foundation, onboarding caches, RLS holes / social RPCs)

All additive (IF NOT EXISTS / DROP POLICY IF EXISTS), safe to re-run.

### Env vars Vercel needs
- `CRON_SECRET` — required by `/api/cron/recompute-reputation` + every cron route now (gate tightened in #405)
- `ETHERSCAN_API_KEY` — used by `/api/wallet/transactions` (decoded EVM activity)
- `HELIUS_API_KEY` — used by `/api/wallet/transactions` (Solana parse)
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY` — fallback when `vapid_keys` table is empty

---

## 3. Carry-forward backlog (everything NOT shipped or merged)

### H6 — VTX + Pino (~4h, BLOCKED on prod-bug diagnosis)
- `vtx-page-streaming` — `/app/dashboard/vtx-ai/page.tsx` SSE wiring. **Built on local branch `fix/h6-vtx-and-pino` (NOT pushed). DO NOT re-attempt until the "/dashboard/vtx-ai half-rendered" mystery is solved — could be related.**
- `vtx-tool-event-streaming` — SSE `tool_use` events from `/api/vtx-ai`, VtxToolSidecar real-time consumption.
- `vtx-portfolio-context` — inject `User Portfolio: N tokens, $X` into the system prompt for authenticated users.
- `vtx-bubble-on-page` — copy BubbleVisualization render block from VtxAiTab to the page-level component.
- `pino-top-20-routes` — convert top-20 highest-frequency console.* routes to `lib/logger.ts`. Top-7 done on the local branch; remaining 13: copy-trading/execute, wallet/* family, vtx-ai/* family.

### H10 leftovers (most shipped via #402, these still pending)
- 20 critical-flow browser smoke tests — specs written in `tests/critical-flows/*.spec.md`, actual Playwright runs pending. Owner runs locally or via Vercel preview.
- 6 section-10 security tests — specs in `tests/security/section-10-security.spec.md`. Same status: specs ready, execution pending.
- `scripts/lighthouse-ci.mjs` — wired but no GitHub Action yet. Add `.github/workflows/lighthouse.yml` that runs on PRs.

### From the 20-agent audit (carry-over from Session Q kickoff §3)
Items NOT covered by the eight audit-followup branches. Use these only after the P0 + main backlog is clear:
- `2fa-implementation` — TOTP (otplib) + WebAuthn passkey in ProfileTab.tsx (H4 shipped the replay-onboarding button + GDPR but not 2FA).
- `privacy-panel-rewire` — ProfileTab.tsx:866-917 writes to `user_metadata.privacy_*`; rewire to discrete `profiles.is_private` / `dm_permission` / `show_*` columns.
- `connected-wallets-edit` — `/app/settings/page.tsx:226-233` is read-only; build add/remove/rename UI hitting `user_wallets_v2`.
- `username-from-profile` — FollowersTile reads username from `user.user_metadata.username`; switch to `profiles.username` via Supabase fetch.
- `app/[locale]-surface-migrations` — foundation shipped; per-surface route moves still pending. Playbook in `docs/seo-locale-migration.md`.
- `top-20-hardcoded-strings` — bundle expanded (landing.* namespaces in `en.json`); per-component `useTranslations()` wiring still pending.
- `tolocale-string-locale-param` — 121 `.toLocaleString()` calls. Hardcoded `'en-US'` sweep done (17 files); `lib/i18n/formatters.ts` helper exists for new code. Component-by-component retrofit still pending.
- `dynamic-route-boundaries` — `error.tsx` + `not-found.tsx` on dynamic `[id]` segments (whale detail, cluster detail, share, coin/[chain]/[address], etc.). H3 core covered the root, dynamic segments still pending.
- `hex-sweep-top-5` — quoted-string-literal hex done. Tailwind-bracketed class hex (`text-[#0A1EFF]`) still pending — needs opacity-modifier composition verification per colour.

### Out-of-this-arc backlog (longer-horizon, owner decides when)
- NFT marketplace beyond display (buy/sell from wallet)
- Forward secrecy on DMs (rotate conversation keys monthly)
- Device recovery flow (restore DM keys after session reset)
- Apple/Google Pay integration (currently zero payment provider wired)
- Webhook signature verification for Stripe/Coinbase Commerce when payments go live
- Refund/cancellation path for subscriptions

---

## 4. How to start Session R (literal first actions)

1. Run §0 bootstrap commands.
2. **Tell the owner the P0 emergency-fix branch needs to merge first.** Reference it by name (`fix/emergency-vtx-and-dashboard-revert`).
3. Ask the owner: *"Can you open `/dashboard/vtx-ai` in a browser, hit F12, and share the console errors?"* — we need those logs to fix the half-rendered VTX UI without guessing.
4. Build the TodoWrite list from §3.
5. Pick the next branch by impact × independence (recommended order: H6 once VTX diagnosis lands → 2FA → privacy panel rewire → connected-wallets editor → dynamic route boundaries).
6. Cut, work, audit, push. Don't ask. Don't opine.

---

## 5. After every branch ships

Update this kickoff doc's §2 (state of the world) + §3 (backlog) by checking off completed items + adding any NEW findings discovered while implementing. This file is the rolling source of truth.

When the H6 → H10 → carry-over list is exhausted: write the Session S kickoff, mark this session as completed, and start the next prompt arc.

---

## 6. Emergency rollback path

If something breaks in prod after merge:
- All Session O–Q migrations are additive (no DROP COLUMN, no DROP TABLE, no data migration). Safe to leave applied; the code paths consuming them gate gracefully on absence.
- Each branch is independently revertable via GitHub. Suggested partial-rollback order: revert audit-followup H-branches first (newest) → revert chore/ branches → revert feat/ branches → only revert refactor/ as a last resort.
- Sentry alerts on every error path now have route + req_id tagging (H2 platform shipped this) — search by tag instead of grepping logs.

---

## 7. MCP loading reference

When the session needs Supabase or Vercel tools, load them via ToolSearch:

```text
# Supabase (29 tools when loaded)
ToolSearch query="select:mcp__supabase__list_tables,mcp__supabase__execute_sql,mcp__supabase__apply_migration,mcp__supabase__get_advisors,mcp__supabase__get_logs"

# Vercel (18 tools when loaded)
ToolSearch query="select:mcp__vercel__list_deployments,mcp__vercel__get_deployment,mcp__vercel__get_deployment_build_logs,mcp__vercel__get_runtime_logs,mcp__vercel__deploy_to_vercel"
```

If a Supabase MCP tool returns a denial classifier error on an additive migration, retry once explaining additive-only intent. Owner has standing approval; the classifier is more conservative than the owner.

---

## 8. Required-reading anchors (don't re-discover these every session)

- **Owner identity / payment / Naka token**: see auto-memory `steinz_labs_user.md` + `steinz_labs_naka_token.md`. Don't re-derive.
- **Hard rules**: `CLAUDE.md` at repo root. Re-read on session start if more than 24h elapsed.
- **Branch state**: `git branch -r` is authoritative. The kickoff list above can be stale.
- **DB schema**: `mcp__supabase__list_tables` is authoritative. Migration files can lie about current state.
- **Live API contracts**: `app/api/**/route.ts` is the source of truth. Don't trust docs/* for behavior — they document intent.
- **Prior session handoffs**: `docs/sessions/HANDOFF-session-{A..N}.md` + `SESSION-Q-KICKOFF.md`. Read only when chasing a specific historical decision.

---

Owner: ship and verify. Future-me: merge the P0 fix, get VTX console logs, pick the next branch, follow the rules, don't ask.
