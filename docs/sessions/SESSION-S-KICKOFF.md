# Session S — Auto-Begin Kickoff

> **Read this file first. Do not re-derive anything below.** Run §0 verbatim, then §1 (rules), §2 (what's hot), §3 (carry-forward). No exploratory re-discovery — every fact here is current as of the end of Session R.

---

## 0. Bootstrap (run these EXACTLY, in order)

```bash
cd "c:/Users/DELL LATITUDE 5320/Downloads/steinzlabs"

# 1. Sync main + prune merged remotes
git fetch --all --prune
git checkout main && git pull --ff-only

# 2. Verify identity (CLAUDE.md hard rule — NEVER skip)
git config user.name   # must print: moderator29
git config user.email  # must print: 101205446+moderator29@users.noreply.github.com
# If wrong:
# git config user.name "moderator29"
# git config user.email "101205446+moderator29@users.noreply.github.com"

# 3. List branches the owner still has open
git branch -r | grep -v 'origin/main$' | grep -v HEAD

# 4. Verify tsc is clean BEFORE touching anything
npx tsc --noEmit -p .   # must report 0 errors

# 5. MCP smoke test — load Supabase + Vercel tools ONLY when needed via
# ToolSearch. Don't ToolSearch them on bootstrap; the tool list shows up
# in <system-reminder> at session start.
#   ToolSearch query="select:mcp__supabase__list_tables,mcp__supabase__execute_sql,mcp__supabase__apply_migration"
#   ToolSearch query="select:mcp__vercel__get_deployment_build_logs,mcp__vercel__list_deployments"
```

**Identity check is non-negotiable.** Every commit attribution depends on it. Sessions in the past have shipped with the wrong author and required history rewrites.

---

## 1. Operating rules (auto-applied — do NOT ask, just execute)

### Hard rules (from `CLAUDE.md`, read it once on session start)

- **Author all commits as** `moderator29 <101205446+moderator29@users.noreply.github.com>`. Verify before first commit.
- **NEVER reference Claude / Anthropic / "AI" / "Generated by"** anywhere — commit messages, code, comments, docs, PR descriptions, branch names. No `claude/`, `ai/`, `claude-code/` branch prefixes. No `Co-Authored-By: Claude` trailer. No `🤖 Generated with Claude Code` footer. **This is a strict, irreversible rule.**
- **NEVER commit to main.** Branch + push + STOP. Owner merges via GitHub UI. `gh` CLI is not installed and that's intentional.
- **Conventional Commits**: `feat:` / `fix:` / `refactor:` / `chore:` / `docs:` / `security:` / `perf:` / `test:` / `style:`.
- **No mock/fake/demo data.** Real APIs only (CoinGecko, Alchemy, Helius, GoPlus, Jupiter, 0x, Anthropic, Supabase, Birdeye, Etherscan v2, Honeypot.is, de.fi, RugCheck). If data is unavailable: empty state + error UI, **never fabricated numbers**.
- **No `any` types** without justification. **No `console.log`** in prod code (use `lib/logger.ts` Pino). **No commented-out code.** **No empty `try/catch` blocks.**
- **Address normalisation**: always `lib/utils/addressNormalize.ts`. Never `.toLowerCase()` on a raw wallet/token address — Solana is case-sensitive.
- **Schema gotchas**: `whales.label` (not `name`), `price_alerts.price` (not `target_price`), `user_wallets_v2.wallets` is JSONB with `default_address` separate. Verify against the live DB via `mcp__supabase__list_tables` / `execute_sql` — migration files can be stale.
- **Supabase migrations** via `mcp__supabase__apply_migration` + mirror SQL into `supabase/migrations/`. Blanket approval for additive ops (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX`, `CREATE TABLE`); **destructive ops** (`DROP`, `TRUNCATE` on prod data, force-push to existing PR branches) require explicit confirmation.
- **Never bypass RLS via `service_role`** from a client-callable endpoint without explicit user-id binding.

### Audit + commit-per-section rule (CRITICAL — surfaced in every recent session)

Before EVERY commit:

1. Run `npx tsc --noEmit -p .` — must report 0 errors (baseline is 0 on current main).
2. Inspect what you wrote for: empty `try/catch`, `console.log` in prod, TODOs, hardcoded values, missing loading/error states, broken pagination, memory leaks, N+1 queries.
3. Commit only that section's files. Don't batch unrelated work into one mega-commit.

For larger multi-section prompts the rule is **3–5 phase branches max** ("no too much of branches") — bundle related work; don't make one branch per file change. But each commit inside a phase branch still gets its own audit.

### Branch hygiene (Session R post-mortem)

- ALWAYS `git fetch --all --prune` before cutting a new branch. `git pull` saying "Already up to date" is a lie if your local fetch is stale — you'll cut from old main, hit conflicts on merge.
- When the owner reports a Vercel build failure, FIRST check whether main has unresolved merge markers in any committed file (`grep -rE '^(<<<<<<<|>>>>>>>) ' --include='*.ts' --include='*.tsx'`). That was the Session R root cause — `app/api/vtx-ai/route.ts` had 27 markers committed.

### Mode rules (carry-over from auto-memory)

- **Autonomous mode**: never stop, never ask, never opine. Industry-standard defaults applied silently. Pause only for genuinely destructive non-migration ops (mass branch delete, `DROP` / `TRUNCATE` on prod data, force-push to existing PR branches, dependency major-version bumps).
- **No-stub build-the-pipeline**: UI missing real data = BUILD the server pipeline in the SAME branch (real upstream API). Never park, never stub.
- **Full-prompt extraction**: numbered-section prompts → pull EVERY sub-bullet into the TodoWrite list. Re-scan the prompt before declaring scope complete.
- **Overnight mode**: when owner says "going to bed" / "wake up still working" / "no stopping" — ship every backlog item in single session, status-update only on branch push or genuine blocker.

### Todo-list rules ("how the owner uses todo lists")

- When owner says "todo list" he means **the FULL backlog in plain English**, then HE picks which to ship. NOT a Claude-internal todo.
- Extract every sub-bullet from a multi-section prompt into separate items. Don't summarise at section level.
- Maintain `TodoWrite` live throughout the session — mark `in_progress` before you start, `completed` immediately on finish. One `in_progress` at a time.
- Stale or done items get cleaned up. Never leave a 20-item TodoWrite that's 18-deep into a different scope.

### Quality bar

- Top 1% MEVX/Nansen/Trojan/Maestro grade — **exceed, not match**.
- TS clean + build clean + 0 console errors + sub-1s page load + sub-2s actions before declaring complete.
- WCAG AAA contrast (7:1 normal, 4.5:1 large). Body 15-16px min, weight 500-600.
- Mobile 375 / 393 / 412 + tablet 768 / 1024 + desktop 1440 / 1920 all perfect.
- Log every paid API call (cost visibility).

### Communication

- Casual tone. No corp-speak.
- TodoWrite a real plan before any code.
- Be honest about scope and blockers. "Wired, awaiting smoke test" beats overclaiming.
- Never lead with "great question" / "absolutely" / sycophancy.

### Auto-memory files (already loaded at session start, no need to re-read)

- `steinz_labs_user.md` — owner profile (Phantomfcalls, founder/CEO, casual tone)
- `steinz_labs_rules.md` — real APIs only, top-1% grade, AAA contrast
- `steinz_labs_autonomous_mode.md` — no stopping, no asking, industry defaults
- `steinz_labs_branch_consolidation.md` — 3-5 phase branches max
- `steinz_labs_no_stub_build_pipeline.md` — build the real server pipeline
- `steinz_labs_full_prompt_extraction.md` — every sub-bullet → todo
- `steinz_labs_overnight_mode.md` — ship every backlog item end-to-end
- `steinz_labs_schema_gotchas.md` — column-name surprises
- `steinz_labs_naka_token.md` — ETH-only, `0x6967b9a8c0b14849CFE8f9E5732B401433fD2898`, threshold 1,227,000
- `steinz_labs_brand_icons.md` — W is colour ref; rocket/helmet/pentagon are platform icons
- `steinz_labs_todo_style.md` — full backlog, then owner picks
- `steinz_labs_session_s_kickoff.md` — pointer to THIS file (write it on first run)

---

## 2. State of the world entering Session S

### Branches awaiting merge (verify with `git branch -r`)

The two open branches at session end are a STACK — the second supersedes the first:

1. **`fix/vtx-render-crashes`** — 7 changes that resolve the VTX-AI empty-middle-column and Profile-tab stuck-spinner bugs.
2. **`fix/mobile-responsive-polish`** — cut FROM `fix/vtx-render-crashes`, adds 4 mobile-only CSS fixes on top:
   - `app/dashboard/page.tsx` content `pt-14 lg:pt-[80px]` (no more dead zone)
   - `components/SidebarMenu.tsx` `max-w-[80vw]` (fits narrow phones)
   - `components/ProfileTab.tsx` delete-modal `items-end sm:items-center pb-[env(keyboard-inset-height,0)]` (docks to keyboard)
   - Fixed header `naka-safe-top` + bottom-nav wrapper `naka-safe-bottom` (iOS dynamic-island + home indicator)

**Merge `fix/mobile-responsive-polish` and you get everything.** Don't need to merge `fix/vtx-render-crashes` separately. tsc clean on both.

### Already merged this arc (DO NOT touch unless reverting)

- `#422` fix/critical-bugs-pretest — VTX route.ts conflict markers + Solana deposit + chain mislabel + market UI unification + back button
- `#423` chore/demo-data-sweep
- `#424` feat/intel-whale-context
- `#425` feat/swap-security-warnings — GoPlus honeypot/tax pre-confirm panel
- `#426` feat/whale-pnl-estimated-fallback
- `#427` fix/context-feed-batch
- `#428` fix/deep-dive-followups — whale schema fix + VTX live price + resolver +16 + inline buy
- `#429` fix/deep-dive-round-2 — whale crash guards + VTX price reliability + resolver +8 + NSFW chart + Phantom logo
- `#430` fix/profile-page-stuck
- `#431` fix/trust-score-naming
- `#432` fix/vtx-card-instant-chart
- `#433` fix/wallet-polish
- `#434` fix/whale-tracker-followups
- `#435` fix/turnstile-bypass-clean

### Known prod issues that may resurface after fix/mobile-responsive-polish merges

- **VTX-AI empty middle** — root cause was 5 unguarded `.toFixed()` / `.slice()` / `.toLowerCase()` calls on possibly-undefined fields in tokenCards loaded from Supabase conversation history. ALL 5 are guarded in `fix/vtx-render-crashes`. If empty-middle persists post-merge, open browser console — there's a different throw we missed.
- **Profile tab stuck spinner** — root cause was `lib/preferences/notificationSettings.ts:89` setting `loading=false` without seeding `settings` on `!userId`. Fixed by seeding DEFAULTS. If spinner persists, check NotificationSettingsPanel's `if (loading || !settings)` guard hasn't been re-broken.
- **iOS dynamic-island overlap / home-indicator clip** — handled by `naka-safe-top` / `naka-safe-bottom` in the polish branch. Other surfaces (settings page, alerts page, etc.) may need the same treatment — sweep after merge.

### Migrations applied to prod DB this arc (already LIVE, mirrored to `supabase/migrations/`)

- `2026_05_17_sniper_executions_buy_amount_usd`
- `2026_05_17_telegram_delivery_failures`
- `2026_05_17_vapid_keys`
- (Plus prior arc migrations: social foundation, onboarding caches, RLS holes / social RPCs.)

All additive (`IF NOT EXISTS` / `DROP POLICY IF EXISTS`), safe to re-run.

### Env vars Vercel needs

- `CRON_SECRET` — required by every `/api/cron/*` route
- `ETHERSCAN_API_KEY` — `/api/wallet/transactions` (decoded EVM activity)
- `HELIUS_API_KEY` — `/api/wallet/transactions` (Solana parse)
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY` — push notification fallback when `vapid_keys` table is empty
- `TOTP_ENC_KEY` — 32-byte base64; encrypts 2FA secrets. The current production value was generated in Session R (`49rJ3e4n/ddCm380pEoGxQUALeEb5JO/H1z3yjKI5jk=`). DO NOT rotate without a re-encryption migration — rotating it invalidates every user's 2FA.
- `ANTHROPIC_API_KEY` — VTX agent + intel thesis

---

## 3. Carry-forward backlog (everything NOT shipped or merged)

### Mobile responsive sweep (only top 5 were in `fix/mobile-responsive-polish`)

Agent found 8 total responsive issues. Top 5 shipped, remaining 3 are smaller polish:

- **Sweep `naka-safe-top` / `naka-safe-bottom`** to all other dashboard surfaces with fixed elements: `/dashboard/alerts`, `/dashboard/settings`, `/dashboard/wallet-page`, `/dashboard/swap`, `/dashboard/copy-trading`, `/dashboard/sniper`. Same pattern as `app/dashboard/page.tsx:373` + `:122`.
- **Tablet 768/1024 audit** — most fixes shipped target 375-412px phones; need a sweep at 768px for grid breakage (e.g., `grid-cols-2 lg:grid-cols-4` jumps abruptly; should have a `md:grid-cols-3` between).
- **Animation respects `prefers-reduced-motion`** — framer-motion components don't honour it yet. Wrap in `useReducedMotion` from framer-motion.

### Trust Score still has 3 conflicting meanings on smaller surfaces

`fix/trust-score-naming` (#431) renamed the two biggest offenders (WhaleAlphaCard, ContextFeed) but did NOT touch:

- `lib/trustScore/formula.ts` — deprecated event-scoring; the canonical formula lives in `lib/trust/calculate.ts`. Delete the deprecated file once you've confirmed no callers.
- `wallet-intelligence/page.tsx` — `contractResult.trustScore` from GoPlus should rename to `.securityScore` to match the naming convention.

### Bug 11 — wallet token chart for custom-token symbols (NSFW is fixed, others may not be)

`fix/deep-dive-round-2` (#429) added `'nsfw': '0x8f00…bf19'` to `COMMUNITY_TOKEN_CONTRACTS`. Same pattern needs to be applied to other custom-token symbols the owner might add. Better long-term: route the chart API through the same `resolveTokenChain` + on-chain contract lookup as the market detail page, instead of a hand-maintained map.

### Bug 7c — Explain button scrollbar issue (NEVER LOCATED IN PRIOR SESSIONS)

Owner reported the Explain button in Context Feed lands the user on an AI explanation page scrolled halfway down with no scrollbar. NEITHER previous agent located the Explain landing page. If owner brings this up again, search:

```bash
grep -rn "Explain with VTX\|/explain\|setShowExplain" components app
```

May be feature-flagged off in prod.

### Whale PnL backfill (server-side pipeline work)

`fix/deep-dive-round-2` shipped a CLIENT-side estimated-net-flow fallback when `whales.pnl_30d_usd` / `pnl_7d_usd` are null. The PROPER fix is a server-side FIFO backfill against Alchemy/Helius:

1. Cron route at `/api/cron/recompute-whale-pnl` (doesn't exist yet)
2. For each whale, pull transfer history via Alchemy `getAssetTransfers` / Helius parsed-txs
3. FIFO match buys against sells, compute realized PnL per token, sum per window
4. Write to `whales.pnl_7d_usd` / `pnl_30d_usd` / `win_rate` / `trade_count_30d`

Owner said this was "out of scope for code fixes" in Session R — listed here for transparency. Re-open when the data-pipeline arc starts.

### Out-of-this-arc backlog (longer-horizon, owner decides when)

- NFT marketplace beyond display (buy/sell from wallet)
- Forward secrecy on DMs (rotate conversation keys monthly)
- Device recovery flow (restore DM keys after session reset)
- Apple/Google Pay integration (currently zero payment provider wired)
- Webhook signature verification for Stripe/Coinbase Commerce when payments go live
- Refund/cancellation path for subscriptions
- Browser-flow Playwright execution (specs exist in `tests/critical-flows/`, runner not in CI)

---

## 4. How to start Session S (literal first actions)

1. Run §0 bootstrap commands.
2. Verify `tsc` baseline is 0 errors.
3. Build the `TodoWrite` list from §3 sub-bullets (one entry per bullet, not section-level).
4. Ask the owner: *what's the priority?* He'll pick. If he hands you a multi-section prompt, extract EVERY sub-bullet first, display the full backlog, THEN start.
5. Cut, work, audit, push. Don't ask. Don't opine.

---

## 5. After every branch ships

Update this kickoff doc's §2 (state of the world) + §3 (backlog) by checking off completed items + adding any NEW findings discovered while implementing. This file is the rolling source of truth — Session T should read it cold and have full context.

When the §3 list is exhausted: write the Session T kickoff, archive this as `HANDOFF-session-S.md`, start the next arc.

---

## 6. Emergency rollback path

If something breaks in prod after merge:

- All Session O–R migrations are additive (no `DROP COLUMN`, no `DROP TABLE`, no data migration). Safe to leave applied; the code paths consuming them gate gracefully on absence.
- Each branch is independently revertable via GitHub. Suggested partial-rollback order: revert audit-followup H-branches first (newest) → revert chore/ branches → revert feat/ branches → only revert refactor/ as a last resort.
- Sentry alerts on every error path now have route + req_id tagging — search by tag instead of grepping logs.
- If Vercel build fails post-merge, FIRST `grep -rE '^(<<<<<<<|>>>>>>>) ' --include='*.ts' --include='*.tsx'` for unresolved merge markers (the Session R root cause). Then check env vars (the second most common cause).

---

## 7. MCP loading reference

When the session needs Supabase or Vercel tools, load them via ToolSearch:

```text
# Supabase (29 tools when loaded)
ToolSearch query="select:mcp__supabase__list_tables,mcp__supabase__execute_sql,mcp__supabase__apply_migration,mcp__supabase__get_advisors,mcp__supabase__get_logs"

# Vercel (18 tools when loaded)
ToolSearch query="select:mcp__vercel__list_deployments,mcp__vercel__get_deployment,mcp__vercel__get_deployment_build_logs,mcp__vercel__get_runtime_logs,mcp__vercel__deploy_to_vercel"
```

**Supabase MCP gotcha**: if `apply_migration` returns a denial classifier error on an additive migration, retry once explaining additive-only intent. Owner has standing approval; the classifier is more conservative than the owner.

**Vercel MCP gotcha** (Session R discovered): the OAuth scope must include `team_nakalabs`, not just personal. If `list_teams` returns `[]` and `get_deployment` with `teamId: 'team_nakalabs'` returns 403, tell the owner to re-auth Vercel MCP from Claude Code settings and **explicitly select the nakalabs team workspace** in the OAuth flow.

---

## 8. Required-reading anchors (don't re-discover these every session)

- **Owner identity / payment / Naka token**: see auto-memory `steinz_labs_user.md` + `steinz_labs_naka_token.md`. Don't re-derive.
- **Hard rules**: `CLAUDE.md` at repo root. Re-read on session start if more than 24h elapsed.
- **Branch state**: `git branch -r` is authoritative. The kickoff list above can be stale.
- **DB schema**: `mcp__supabase__list_tables` is authoritative. Migration files can lie about current state.
- **Live API contracts**: `app/api/**/route.ts` is the source of truth. Don't trust `docs/*` for behaviour — they document intent.
- **Prior session handoffs**: `docs/sessions/HANDOFF-session-{A..N}.md` + `SESSION-Q-KICKOFF.md` + `SESSION-R-KICKOFF.md`. Read only when chasing a specific historical decision.

---

Owner: ship and verify. Future-me: read this top-to-bottom on session start, do not ask, do not opine, ship the §3 backlog by impact × independence, audit every commit, push every branch, never touch main directly.
