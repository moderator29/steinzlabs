# Naka Labs — Autonomous Session Handoff (2026-06-24)

**For:** the next Claude Code session (fresh context). **Read this top-to-bottom before touching anything.**
**User:** Phantomfcalls (founder/CEO, brand: Naka Labs / nakalabs.xyz, GitHub: `moderator29`).
**Repo:** `C:\Users\DELL LATITUDE 5320\Downloads\steinzlabs` (Next.js 16, Supabase, Vercel auto-deploys `main`).

---

## 0 · YOUR MANDATE — work fully autonomously

The user wants you to **work the backlog autonomously — really autonomously.** Do NOT stop after each
item to ask "what next." The loop is:

> **pick the next item → do it → self-audit it (re-grep, re-tsc, re-build) → fix any incidental issue you
> spot even if it's not your task → commit cleanly → push → next item.**

- One branch per task, cut from `main`. Push and STOP at the PR — the user merges in the GitHub UI.
- Only pause for the explicit human-gated items (live-DB migrations on real data, real-money signing paths,
  the `CRONS_PAUSED` env flip, the Vercel reconnect). Everything else: just do it.
- Keep a clean TodoWrite list and update it as you go.
- Verify against the LIVE Supabase DB before any schema claim — migration files are stale.

---

## 1 · NON-NEGOTIABLE RULES (load these every commit)

**Commit identity / attribution (the most important rules):**
- Author = `moderator29 <101205446+moderator29@users.noreply.github.com>` ONLY. Git is already configured.
- **ZERO AI attribution.** No `Co-Authored-By: Claude`, no `🤖 Generated with Claude Code`, no
  `Generated by/AI-assisted` — not in commits, PRs, code, comments, or docs. Use HEREDOC commits; strip the
  default Claude Code trailer/footer every time.
- No `claude/`, `ai/`, `claude-code/` branch prefixes. Use `feat/`, `fix/`, `docs/`, `chore/`, `refactor/`, `security/`.
- Conventional Commits (`type(scope): imperative subject`), body explains WHY not what.
- **Never commit to `main`.** Never `--no-verify` / `--no-gpg-sign`. Never force-push (sandbox blocks it).
- Never amend a published commit — make a new commit on top.

**Code / product quality:**
- **NO mock/fake/demo data. Real APIs only** (CoinGecko, DexScreener, Alchemy, Helius, GoPlus, Jupiter, 0x,
  Anthropic, Supabase). If data is unavailable → honest empty state, never a fabricated number. (This is the
  single biggest theme of the audits — fabrication = the top reputational risk.)
- `lib/utils/addressNormalize.ts` for ALL address comparisons. Never `.toLowerCase()` a wallet/token address
  (Solana is case-sensitive).
- No `any` (unless documented), no `console.log` in prod, no empty try/catch, no commented-out code.
- WCAG AAA on text contrast. Don't refactor/abstract beyond what the task needs. Default to no comments
  unless the WHY is non-obvious.
- Real-money path = non-custodial only. Never server-sign trades; signing flows through the browser.

**Supabase gotchas:** `whales.label` (not name), `price_alerts.price` (not target_price),
`user_wallets_v2.wallets` JSONB. ALWAYS verify columns against the live DB (`mcp__supabase__list_tables` /
`execute_sql`), never from migration files.

---

## 2 · BOOTSTRAP

- **Repo:** `C:\Users\DELL LATITUDE 5320\Downloads\steinzlabs`. Node 20+, npm. Windows 11, PowerShell 5.1.
  Prefer the **Bash** tool for sweeps (PowerShell 5.1 mangles em-dashes / lacks `&&`). CI repro:
  `npm ci --legacy-peer-deps && npx tsc --noEmit` must exit 0. Local `next build` fails on page-data
  collection unless `RESEND_API_KEY` is set — use `RESEND_API_KEY=re_dummy_build npm run build` to validate
  locally (real value lives only in Vercel; prod builds fine).
- **MCPs:** Supabase ✅ connected (project `phvewrldcdxupsnakddx`, ACTIVE_HEALTHY). **Vercel ❌ scope-broken**
  (`list_teams` returns empty, `list_projects` 403s) — don't fight it; ask the user to paste a deploy/preview
  URL if you need runtime data, or run `next dev` locally for visual QA.
- **tsc tip:** after switching branches, `rm -rf .next` before `npx tsc --noEmit` — stale `.next/types`
  reference files from the other branch and produce phantom errors.

---

## 3 · TWO BLOCKERS THAT NEED THE USER (flag, don't attempt)

1. **🔴 `CRONS_PAUSED` env on Vercel** — the entire cron fleet has been frozen since **2026-05-13**
   (`app/api/cron/_shared.ts:10` global kill switch). This is the upstream cause of ~30% of the audit
   findings (empty whale feed, stale reputation, no pricing, no PnL backfill). Ask the user to check Vercel →
   steinzlabs → Settings → Environment Variables → `CRONS_PAUSED`; set false / delete + redeploy. Single
   biggest win on the board. **Many P0/P1 whale + reputation items are blocked on this.**
2. **Vercel MCP reconnect** with personal + Naka Labs team scope (team `team_YiyNREYxlCCmV9Zx9JQmFbCU`),
   needed for runtime-error reading and visual QA.

---

## 4 · WHAT SHIPPED THIS SESSION (don't redo)

**Merged to `main` already:** Phase 2 (palette→`#0066FF`, light-mode, theme), notification_settings dup
retire, swap-fee logging (§25), transactions-page reader, whale-backfill-pnl cron, **the VTX cards + swap
branch** (`feat/cards-swap-ui-upgrade`, PR #584): price-card UI polish, **CoinGecko-first pricing**
(majors from CoinGecko — fixed SOL showing $95 vs real ~$67 — DexScreener for memecoins/pump.fun/four.meme),
swap wallet-connect gating (AppKit `open()` + Naka-wallet link), swap-card visual elevation.

**Pushed, awaiting user merge (7 mine + 2 others):**
`fix/leaderboard-watchers-and-follow` (300%→count + i_follow) · `fix/market-filters-button` (wired dead
Filters) · `fix/nav-toggle-and-name-display` (visible hamburger + greet by first name) · `fix/settings-dead-links`
(redirects) · `fix/tier-badge-colors` (mini/naka_cult) · `fix/transaction-history-upsert` (conflict target +
user_id) · `fix/view-proof-fake-ai` (retire fabricated "AI analysis") · plus `feat/passkey-unlock-full`,
`feat/social-notifications` (not mine).

**Reference docs (the full backlog lives here):**
- `docs/sessions/AUDIT-2026-06-24-22agent.md` — the 182-finding 22-agent audit (P0→P3 + 32-item SQL worklist).
- `docs/sessions/MASTER-TODO-remaining-2026-06-24.md` — old 16-agent audit expanded (Phase 2-5 + SQL).

---

## 5 · THE REMAINING BACKLOG (work it top-down, autonomously)

> Full detail + file:line evidence is in the two audit docs above. This is the prioritized worklist.

### P0 — trust / money / data-integrity
- **Un-freeze crons** (env flip — user). Add a `last_run` health check + failure alerting once running.
- **`whale-backfill-pnl`** already fixed; verify after crons run. Check other crons that log `success` on
  total failure (`copy-trade-monitor`).
- **Real swap execution** — `InlineBuySellForm.tsx:224` prints "Bought/Sold" without signing; `swap/page.tsx:1029`
  sets `confirmed` on broadcast before any receipt. Sign→broadcast→**poll receipt**→confirm on `status===1`.
  *(real-money — needs the user's wallet to test before merge.)*
- **Swap `amountIn` decimals** — sent in whole USD → ~1e6 off for USDC. `sellAmount = BigInt(usd*10**dec)`.
- **Create `/api/swap/execute`** — 404 today; `SwapCard` + `useSwapExecution` POST to it.
- **MEV honesty** — `mevProtect` is theater (broadcasts public). Integrate Flashbots/Jito OR remove the claim.
- **swap status lifecycle** — PATCH `swap_logs.status`/`tx_hash` after confirmation; wire dead `recordSwapCompletion`.
- **whale `value_usd` backfill** — 53,929 rows 100% NULL → feed returns 0 (`feed/route.ts:90`). After crons.
- **Kill fabricated whale data** — `pnl == whale_score × $1M`; a16z `$97M`; synthetic 24h volume (`top-today:58`).
- **Reputation scorer** queries non-existent `swap_logs.pnl_usd` (`scorer.ts:138`) → top-traders empty.
- **notification_settings** dup already retired; the canonical profile panel works.

### P1 — broken core
- **`.toLowerCase()` on addresses** — 8 real sites in `app/dashboard/wallet-page/page.tsx`
  (lines 150, 310-311, 551, 762-764, 1136, 2133) → `addressNormalize`. (Lines 506/776/797/1530 are chain-ids/
  symbols — leave them.) Per-site chain context needed; careful work.
- **Add-token end-to-end** — chain-prefixed key + chain selector + real ERC20/SPL balance + chain-aware GoPlus
  scan (`wallet-page:548-550,723,2116`).
- **Whale**: normalize `whale_activity.action` to {buy,sell,transfer} (100% `transfer_out`); map
  `whales.entity_type` ↔ feed label taxonomy; gate the always-on "Live" badge on freshness; Solana whale indexer.
- **Follow/copy delivery** — alerts-only follow mode delivers ZERO alerts; `copy-trade-monitor` probes
  non-existent `copy_trade_rules` (→ `user_copy_rules`); normalize copy-mode enum.
- **Solana/non-0x swap** — Solana branch in `/api/swap/quote`; fix non-0x route body+txHash; SwapCard quote params.
- **DM realtime** — call `supabase.realtime.setAuth(token)` before subscribe (RLS blocks all events today);
  read receipts; **DM key-wrap off the rotating JWT** (`encryption.ts:60` → WebAuthn PRF / passphrase / IndexedDB).
- **sniper security_score:null** — criteria `min_security_score>0` never match; GoPlus enrich pass.

### P2 — missing
- **Dune tier** — 17 `dune_*` tables never applied to live DB; 6 UI strips + 5 crons read into the void.
  Apply both migrations (fix invalid `CREATE POLICY IF NOT EXISTS` first) + `DUNE_API_KEY` + query IDs, OR add
  has-data guards + disable the crons.
- DM recipient picker/compose/unread; **`transactions` table writer** on swap/send (read by ~10 features,
  written by nothing); leaderboards `/_all` batch or materialized view; settings persistence
  (security-prefs UI, `user_trading_preferences`, language round-trip); follow "Follows you" + optimistic.

### P3 — polish
- TierBadge on `/u/[username]` header; market sortable headers + delete orphaned components + stop fabricated
  50/50 ratio; dead-filter cleanup (clusters Member Count sort, Smart Money toggles, wallet "By Change" sort);
  gate Encrypted/Live badges on real state; GDPR export all 7 preference tables; branded `not-found.tsx`/`error.tsx`,
  favicon/OG/twitter images; the 9 stub crons (alert-monitor etc.) implement-or-remove.

### Old-audit Phase 2/5 leftovers
- §12 migrate chrome offenders to `components/ui` primitives + eslint guard; §17 one i18n path (next-intl) +
  persist language; §34-36 unify admin auth (httpOnly + `admin_roles` middleware) + admin settings/treasury/
  broadcast on real columns.

### Schema worklist (verify live first; mirror migrations into `supabase/migrations/`)
`lower(username)` unique + dedupe Puffnutz; `followers_count`/`following_count` + triggers; `user_custom_tokens`;
`funding_rate_snapshots` (UNIQUE exchange,symbol); `goplus_security_cache`; reconcile `sniper_executions`
(`executed_at`/`buy_amount_usd`) + `notification_settings` reads; **CI insert-roundtrip contract test** for
`swap_logs`/`fee_revenue`/`transactions` so drift fails the build.

---

## 6 · KEY TECHNICAL CONTEXT (so you don't relearn it)

- **Schema-drift pattern (the recurring bug):** code inserts/reads column names that don't exist on the live
  table; the error is caught + swallowed; the UI reports success. Fix once with the live column names + a shared
  TS type; stop swallowing insert errors. Verified-fixed: `swap_logs`/`fee_revenue` writers, transactions-page
  reader, `transaction_history` upsert, whale-backfill-pnl, notification_settings.
- **Price sourcing (just shipped):** majors → CoinGecko (`getMarketsByIds` / `/coins/markets`, authoritative
  price + stats), no contract address so the client's symbol path stays on CoinGecko; long-tail/memecoins → by
  address through DexScreener. The VTX card live data comes from `/api/vtx/token-card` (symbol path =
  CoinGecko, address path = DexScreener) — not the initial `tokenCard`.
- **Wallet:** `useNakaWallet()` is read-only state (no connect method). Connect via AppKit `useAppKit().open()`
  (WalletConnect/external) or the built-in Naka wallet on `/dashboard/wallet-page`. VTX wallet-holdings access
  is a separate profile toggle (`user_preferences.vtx_wallet_access`, default off).
- **VTX cards:** `components/market/PriceCard.tsx` (the big intel card) and `components/vtx/SwapCard.tsx`
  (540 lines, quote→Sign&Swap flow). Shared chart = `components/market/SparklineChart.tsx` (lightweight-charts;
  `attributionLogo:false` already set to kill the TradingView watermark).

---

## 7 · OPENING SEQUENCE

```bash
cd "C:\Users\DELL LATITUDE 5320\Downloads\steinzlabs"
git fetch --prune origin && git checkout main && git pull --ff-only
npm ci --legacy-peer-deps && npx tsc --noEmit      # baseline must exit 0
# Verify MCPs: mcp__supabase__list_projects (✅). Vercel likely still 403 — ask user / use local dev.
# Read docs/sessions/AUDIT-2026-06-24-22agent.md for the full P0→P3 detail.
# Then start at §5 P0 and hammer down autonomously: do → self-audit → fix incidentals → commit → push → next.
```

**Top of mind:** (1) ask the user to flip `CRONS_PAUSED` first — it unblocks the whole whale/reputation/pricing
tier. (2) Real-money swap items need the user's wallet to test before merge. (3) Everything else: just go.
